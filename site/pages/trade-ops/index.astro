---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import DomainShell from '../../components/DomainShell.astro';
import { entriesByDomain, getDomainMeta, visibleEntries } from '../../lib/content';

const meta = getDomainMeta('trade_ops')!;
const entries = entriesByDomain(visibleEntries(await getCollection('practice')), 'trade_ops');
---
<BaseLayout title={meta.name} description={meta.summary}>
  <DomainShell meta={meta} entries={entries} />
</BaseLayout>
